import { ShopRecommendation } from './shop-recommendation.model';

describe('ShopRecommendation', () => {
  it('should create an instance', () => {
    expect(new ShopRecommendation()).toBeTruthy();
  });
});
